{
  "name": "art-journey",
  "version": "1.0.0",
  "type": "module",
  "description": "AR Art Gallery",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp dist/index.html dist/200.html && cp ar.html dist/ && surge dist artjourney.surge.sh"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "vite": "^5.0.0"
  }
}
